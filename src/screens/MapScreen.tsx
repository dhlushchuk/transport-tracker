import { View } from "react-native";
import TransportFullMap from "../components/TransportFullMap";
import type { FC } from "react";

const MapScreen: FC = () => {
  return (
    <View>
      <TransportFullMap />
    </View>
  );
};

export default MapScreen;
